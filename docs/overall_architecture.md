# KineSentry Overall Architecture

This diagram captures the implemented end-to-end architecture across the wearable glove, the hub, the Flutter app, and its cloud/local service dependencies.

```mermaid
flowchart LR
  classDef sensor fill:#FFF4DB,stroke:#E0A100,stroke-width:1.5px,color:#3B2A00
  classDef device fill:#E8F2FF,stroke:#2563EB,stroke-width:2px,color:#0F172A
  classDef compute fill:#EAFBF1,stroke:#16A34A,stroke-width:1.5px,color:#052E16
  classDef app fill:#F3E8FF,stroke:#7C3AED,stroke-width:2px,color:#2E1065
  classDef cloud fill:#FFEAEA,stroke:#DC2626,stroke-width:1.5px,color:#450A0A
  classDef platform fill:#F4F4F5,stroke:#52525B,stroke-width:1.2px,color:#18181B
  classDef actor fill:#FFF,stroke:#334155,stroke-width:1.2px,color:#0F172A

  User["Caregiver / User"]:::actor
  Patient["Patient Wearing Smart Glove"]:::actor

  subgraph G["Smart Glove Device"]
    direction TB
    G1["MAX30102
    Heart Rate + SpO2"]:::sensor
    G2["LM35
    Body Temperature"]:::sensor
    G3["MPU6050
    Motion / Tilt / Fall"]:::sensor
    G4["ESP32 Glove Controller
    Reads sensors
    Detects gestures
    Detects fall
    Calculates battery"]:::device
    G5["Local Output
    OLED Display
    Buzzer
    Status LED"]:::compute
    G6["Deep Sleep Control"]:::compute
    G1 --> G4
    G2 --> G4
    G3 --> G4
    G4 --> G5
    G4 --> G6
  end

  subgraph H["Smart Hub Device"]
    direction TB
    H1["ESP32 Hub Receiver
    Receives glove packets"]:::device
    H2["Hub Processing
    Smooths battery
    Controls alerts
    Maintains glove link"]:::compute
    H3["BLE GATT Server
    Notify to app
    Write from app"]:::device
    H4["Hub Output
    OLED Display
    LEDs
    Buzzer
    Pair Button"]:::compute
    H5["Hub Deep Sleep"]:::compute
    H1 --> H2 --> H3
    H2 --> H4
    H2 --> H5
  end

  subgraph A["Flutter Mobile / Desktop App"]
    direction TB
    A1["Bluetooth Layer
    Scan
    Connect
    Listen
    Send Commands"]:::app
    A2["Parser + Data Service
    Parse BLE payload
    Store live data
    Session history
    Alert history"]:::app
    A3["Health Rules + ML Insight
    Status checks
    Risk scoring
    Trend prediction"]:::app
    A4["UI Screens
    Dashboard
    Alerts
    Bluetooth
    Reports
    Settings"]:::app
    A5["Alert Layer
    Voice
    Sound
    Notifications"]:::app
    A6["Reports Layer
    PDF generation
    Firestore report sync"]:::app
    A7["Settings + Session Control
    Deep sleep toggle
    Dummy data
    Logout flow"]:::app
    A1 --> A2 --> A3 --> A4
    A2 --> A5
    A2 --> A6
    A7 --> A1
    A7 --> A4
  end

  subgraph C["Cloud Backend"]
    direction TB
    C1["Firebase Auth"]:::cloud
    C2["Cloud Firestore
    users/{uid}/reports"]:::cloud
  end

  subgraph P["Phone / OS Services"]
    direction TB
    P1["Local Notifications"]:::platform
    P2["Text To Speech"]:::platform
    P3["App Sound Player"]:::platform
    P4["PDF View / Share"]:::platform
    P5["SharedPreferences"]:::platform
    P6["Bluetooth Speaker / Headset"]:::platform
  end

  Patient --> G
  User --> A4

  G4 == "ESP-NOW sensor packet
  BPM, SpO2, Temp, Gesture, Fall, Battery" ==> H1
  H3 == "BLE notification stream
  live vitals + state" ==> A1

  A1 -. "SLEEP_MODE
  SPEAKER_ACK
  APP_LOGOUT" .-> H3
  H2 -. "Deep sleep control" .-> G4

  A5 --> P1
  A5 --> P2
  A5 --> P3
  A6 --> P4
  A7 --> P5
  A5 --> P6

  A4 --> C1
  A6 --> C2
  C1 --> A4
```

## Notes

- Primary live data path: `Glove sensors -> ESP-NOW -> Hub -> BLE -> Parser -> DataService -> Dashboard / Alerts / Reports / ML`.
- Primary control path: `Settings / Session actions in app -> BLE write commands -> Hub -> ESP-NOW control -> Glove`.
- Reports are regenerated in-app from Firestore samples and then viewed/shared as PDFs.
- The current BLE payload generated by the hub includes `speaker` and `sleep`. The Bluetooth screen also checks for `speaker_request`, which does not appear in the current hub payload and may represent an integration gap.
